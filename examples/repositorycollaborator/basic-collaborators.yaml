---
# Read-only collaborator
apiVersion: repositorycollaborator.gitea.m.crossplane.io/v2
kind: RepositoryCollaborator
metadata:
  name: readonly-collaborator
  namespace: default
spec:
  forProvider:
    username: john-doe
    repository: my-example-org/my-repo
    permission: read
  providerConfigRef:
    name: default
---
# Write access collaborator
apiVersion: repositorycollaborator.gitea.m.crossplane.io/v2
kind: RepositoryCollaborator
metadata:
  name: write-collaborator
  namespace: default
spec:
  forProvider:
    username: jane-smith
    repository: my-example-org/my-repo
    permission: write
  providerConfigRef:
    name: default
---
# Admin collaborator
apiVersion: repositorycollaborator.gitea.m.crossplane.io/v2
kind: RepositoryCollaborator
metadata:
  name: admin-collaborator
  namespace: default
spec:
  forProvider:
    username: admin-user
    repository: my-example-org/my-repo
    permission: admin
  providerConfigRef:
    name: default
---
# Personal repository collaborator
apiVersion: repositorycollaborator.gitea.m.crossplane.io/v2
kind: RepositoryCollaborator
metadata:
  name: personal-repo-collaborator
  namespace: default
spec:
  forProvider:
    username: contributor
    repository: user123/personal-project
    permission: write
  providerConfigRef:
    name: default
