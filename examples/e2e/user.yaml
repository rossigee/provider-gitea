apiVersion: user.gitea.m.crossplane.io/v2
kind: User
metadata: {name: uptest-user, namespace: provider-gitea-e2e, annotations: {meta.upbound.io/example-id: user/v2/user}}
spec:
  forProvider:
    username: uptest-user
    email: uptest-user@example.com
    password: ChangeMe-123
  providerConfigRef: {name: e2e, kind: ProviderConfig}
