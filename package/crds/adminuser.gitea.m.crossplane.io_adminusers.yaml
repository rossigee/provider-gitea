---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: adminusers.adminuser.gitea.m.crossplane.io
spec:
  group: adminuser.gitea.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gitea
    kind: AdminUser
    listKind: AdminUserList
    plural: adminusers
    shortNames:
    - admi
    singular: adminuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v2
    schema:
      openAPIV3Schema:
        description: AdminUser is the Schema for the adminusers API v2 (namespaced)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AdminUserSpec defines the desired state of AdminUser
            properties:
              forProvider:
                properties:
                  connectionRef:
                    description: |-
                      V2 Enhancement: Connection reference for multi-tenant support
                      ConnectionRef specifies the Gitea connection to use
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  description:
                    description: Description is the user's bio/description
                    maxLength: 255
                    type: string
                  email:
                    description: Email is the user's email address
                    format: email
                    type: string
                  fullName:
                    description: FullName is the user's display name
                    maxLength: 100
                    type: string
                  isActive:
                    default: true
                    description: IsActive indicates if the user account is active
                    type: boolean
                  isAdmin:
                    default: false
                    description: IsAdmin indicates if the user should have admin privileges
                    type: boolean
                  isRestricted:
                    default: false
                    description: IsRestricted indicates if the user has restricted
                      access
                    type: boolean
                  location:
                    description: Location is the user's location
                    maxLength: 50
                    type: string
                  maxRepoCreation:
                    default: -1
                    description: MaxRepoCreation limits the number of repositories
                      the user can create (-1 for unlimited)
                    minimum: -1
                    type: integer
                  mustChangePassword:
                    default: false
                    description: MustChangePassword forces the user to change password
                      on first login
                    type: boolean
                  passwordSecretRef:
                    description: PasswordSecretRef references a Kubernetes secret
                      containing the user's password
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  prohibitLogin:
                    default: false
                    description: ProhibitLogin prevents the user from logging in (useful
                      for bot accounts)
                    type: boolean
                  providerConfigRef:
                    description: |-
                      V2 Enhancement: Namespace-scoped provider config
                      ProviderConfigRef references a ProviderConfig resource in the same namespace
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  sendNotify:
                    default: false
                    description: SendNotify sends a notification email to the user
                    type: boolean
                  username:
                    description: Username is the user's login name
                    maxLength: 40
                    minLength: 1
                    pattern: ^[a-zA-Z0-9]([a-zA-Z0-9._-]*[a-zA-Z0-9])?$
                    type: string
                  visibility:
                    default: public
                    description: Visibility controls the user's profile visibility
                    enum:
                    - public
                    - private
                    - limited
                    type: string
                  website:
                    description: Website is the user's website URL
                    format: uri
                    type: string
                required:
                - email
                - passwordSecretRef
                - username
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: AdminUserStatus defines the observed state of AdminUser
            properties:
              atProvider:
                properties:
                  avatarUrl:
                    description: AvatarURL is the URL to the user's avatar
                    type: string
                  createdAt:
                    description: CreatedAt is the timestamp when the user was created
                    type: string
                  description:
                    description: Description is the user's bio/description
                    type: string
                  email:
                    description: Email is the user's email address
                    type: string
                  fullName:
                    description: FullName is the user's display name
                    type: string
                  id:
                    description: ID is the unique identifier of the user
                    format: int64
                    type: integer
                  isActive:
                    description: IsActive indicates if the user account is active
                    type: boolean
                  isAdmin:
                    description: IsAdmin indicates if the user has admin privileges
                    type: boolean
                  isRestricted:
                    description: IsRestricted indicates if the user has restricted
                      access
                    type: boolean
                  language:
                    description: Language is the user's preferred language
                    type: string
                  lastLogin:
                    description: LastLogin is the timestamp of the user's last login
                    type: string
                  location:
                    description: Location is the user's location
                    type: string
                  maxRepoCreation:
                    description: MaxRepoCreation is the user's repository creation
                      limit
                    type: integer
                  prohibitLogin:
                    description: ProhibitLogin indicates if the user is prevented
                      from logging in
                    type: boolean
                  userStats:
                    description: UserStats contains user statistics
                    properties:
                      totalOrganizations:
                        description: TotalOrganizations is the total number of organizations
                        format: int64
                        type: integer
                      totalRepositories:
                        description: TotalRepositories is the total number of repositories
                        format: int64
                        type: integer
                      totalUsers:
                        description: TotalUsers is the total number of users
                        format: int64
                        type: integer
                    type: object
                  username:
                    description: Username is the user's login name
                    type: string
                  visibility:
                    description: Visibility shows the user's profile visibility
                    type: string
                  website:
                    description: Website is the user's website URL
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
