---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: runners.runner.gitea.m.crossplane.io
spec:
  group: runner.gitea.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gitea
    kind: Runner
    listKind: RunnerList
    plural: runners
    shortNames:
    - runn
    singular: runner
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v2
    schema:
      openAPIV3Schema:
        description: Runner is the Schema for the runners API v2 (namespaced)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RunnerSpec defines the desired state of Runner
            properties:
              forProvider:
                properties:
                  connectionRef:
                    description: |-
                      V2 Enhancement: Connection reference for multi-tenant support
                      ConnectionRef specifies the Gitea connection to use
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  description:
                    description: Description is an optional description for the runner
                    maxLength: 500
                    type: string
                  labels:
                    description: Labels are the labels assigned to this runner for
                      job targeting
                    items:
                      type: string
                    minItems: 1
                    type: array
                  name:
                    description: Name is the display name for the runner
                    maxLength: 100
                    minLength: 1
                    type: string
                  providerConfigRef:
                    description: |-
                      V2 Enhancement: Namespace-scoped provider config
                      ProviderConfigRef references a ProviderConfig resource in the same namespace
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  runnerGroupId:
                    description: RunnerGroupID is the ID of the runner group (for
                      organization/system runners)
                    format: int64
                    type: integer
                  scope:
                    description: Scope defines where the runner is registered (repository,
                      organization, or system)
                    enum:
                    - repository
                    - organization
                    - system
                    type: string
                  scopeValue:
                    description: |-
                      ScopeValue is the repository (owner/name) or organization name for the scope
                      Required for repository and organization scopes, ignored for system scope
                    pattern: ^([a-zA-Z0-9]([a-zA-Z0-9._-]*[a-zA-Z0-9])?(/[a-zA-Z0-9]([a-zA-Z0-9._-]*[a-zA-Z0-9])?)?)?$
                    type: string
                required:
                - labels
                - name
                - scope
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: RunnerStatus defines the observed state of Runner
            properties:
              atProvider:
                properties:
                  architecture:
                    description: Architecture is the runner system architecture
                    type: string
                  createdAt:
                    description: CreatedAt is the timestamp when the runner was registered
                    type: string
                  description:
                    description: Description is the runner description
                    type: string
                  id:
                    description: ID is the unique identifier of the runner
                    format: int64
                    type: integer
                  labels:
                    description: Labels are the current labels assigned to this runner
                    items:
                      type: string
                    type: array
                  lastOnline:
                    description: LastOnline is the timestamp when the runner was last
                      online
                    type: string
                  name:
                    description: Name is the display name for the runner
                    type: string
                  operatingSystem:
                    description: OperatingSystem is the runner OS
                    type: string
                  runnerGroup:
                    description: RunnerGroup contains runner group information
                    properties:
                      description:
                        description: Description is the group description
                        type: string
                      id:
                        description: ID is the group identifier
                        format: int64
                        type: integer
                      name:
                        description: Name is the group name
                        type: string
                    type: object
                  scope:
                    description: Scope indicates where the runner is registered
                    type: string
                  scopeValue:
                    description: ScopeValue is the repository or organization name
                    type: string
                  status:
                    description: Status indicates the runner status (online, offline,
                      idle, active)
                    type: string
                  tokenExpiresAt:
                    description: TokenExpiresAt is when the runner token expires
                    type: string
                  updatedAt:
                    description: UpdatedAt is the timestamp when the runner was last
                      updated
                    type: string
                  uuid:
                    description: UUID is the unique UUID of the runner
                    type: string
                  version:
                    description: Version is the runner agent version
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
