---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: branchprotections.branchprotection.gitea.m.crossplane.io
spec:
  group: branchprotection.gitea.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gitea
    kind: BranchProtection
    listKind: BranchProtectionList
    plural: branchprotections
    shortNames:
    - bran
    singular: branchprotection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v2
    schema:
      openAPIV3Schema:
        description: BranchProtection is the Schema for the branchprotections API
          v2 (namespaced)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BranchProtectionSpec defines the desired state of BranchProtection
            properties:
              forProvider:
                properties:
                  approvalsWhitelistTeams:
                    description: ApprovalsWhitelistTeams is the list of teams allowed
                      to approve
                    items:
                      type: string
                    type: array
                  approvalsWhitelistUsernames:
                    description: ApprovalsWhitelistUsernames is the list of usernames
                      allowed to approve
                    items:
                      type: string
                    type: array
                  blockOnOfficialReviewRequests:
                    default: false
                    description: BlockOnOfficialReviewRequests blocks merge when official
                      review is requested
                    type: boolean
                  blockOnOutdatedBranch:
                    default: false
                    description: BlockOnOutdatedBranch blocks merge when branch is
                      outdated
                    type: boolean
                  blockOnRejectedReviews:
                    default: false
                    description: BlockOnRejectedReviews blocks merge when there are
                      rejected reviews
                    type: boolean
                  branch:
                    description: Branch is the branch name to protect
                    type: string
                  connectionRef:
                    description: |-
                      V2 Enhancement: Connection reference for multi-tenant support
                      ConnectionRef specifies the Gitea connection to use
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dismissStaleApprovals:
                    default: false
                    description: DismissStaleApprovals dismisses stale approvals when
                      new commits are pushed
                    type: boolean
                  enableApprovalsWhitelist:
                    default: false
                    description: EnableApprovalsWhitelist enables approval whitelist
                    type: boolean
                  enableMergeWhitelist:
                    default: false
                    description: EnableMergeWhitelist enables merge whitelist
                    type: boolean
                  enablePush:
                    default: true
                    description: EnablePush controls whether pushes are allowed
                    type: boolean
                  enablePushWhitelist:
                    default: false
                    description: EnablePushWhitelist enables push whitelist
                    type: boolean
                  enableStatusCheck:
                    default: false
                    description: EnableStatusCheck enables status check requirements
                    type: boolean
                  mergeWhitelistTeams:
                    description: MergeWhitelistTeams is the list of teams allowed
                      to merge
                    items:
                      type: string
                    type: array
                  mergeWhitelistUsernames:
                    description: MergeWhitelistUsernames is the list of usernames
                      allowed to merge
                    items:
                      type: string
                    type: array
                  protectedFilePatterns:
                    description: ProtectedFilePatterns defines patterns for files
                      that require special protection
                    type: string
                  providerConfigRef:
                    description: |-
                      V2 Enhancement: Namespace-scoped provider config
                      ProviderConfigRef references a ProviderConfig resource in the same namespace
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  pushWhitelistDeployKeys:
                    default: false
                    description: PushWhitelistDeployKeys allows deploy keys to push
                    type: boolean
                  pushWhitelistTeams:
                    description: PushWhitelistTeams is the list of teams allowed to
                      push
                    items:
                      type: string
                    type: array
                  pushWhitelistUsernames:
                    description: PushWhitelistUsernames is the list of usernames allowed
                      to push
                    items:
                      type: string
                    type: array
                  repository:
                    description: Repository is the repository that owns this branch
                      protection rule (owner/name format)
                    pattern: ^[a-zA-Z0-9]([a-zA-Z0-9._-]*[a-zA-Z0-9])?/[a-zA-Z0-9]([a-zA-Z0-9._-]*[a-zA-Z0-9])?$
                    type: string
                  requireSignedCommits:
                    default: false
                    description: RequireSignedCommits requires all commits to be signed
                    type: boolean
                  requiredApprovals:
                    default: 0
                    description: RequiredApprovals is the number of required approvals
                    maximum: 100
                    minimum: 0
                    type: integer
                  ruleName:
                    description: RuleName is the name of the protection rule
                    type: string
                  statusCheckContexts:
                    description: StatusCheckContexts is the list of required status
                      check contexts
                    items:
                      type: string
                    type: array
                  unprotectedFilePatterns:
                    description: UnprotectedFilePatterns defines patterns for files
                      that bypass protection
                    type: string
                required:
                - branch
                - repository
                - ruleName
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: BranchProtectionStatus defines the observed state of BranchProtection
            properties:
              atProvider:
                properties:
                  appliedSettings:
                    description: Applied settings summary
                    properties:
                      enablePush:
                        description: EnablePush indicates if push protection is enabled
                        type: boolean
                      enableStatusCheck:
                        description: EnableStatusCheck indicates if status checks
                          are required
                        type: boolean
                      requiredStatusChecks:
                        description: RequiredStatusChecks lists the required status
                          checks
                        items:
                          type: string
                        type: array
                    type: object
                  createdAt:
                    description: CreatedAt is the timestamp when the rule was created
                    type: string
                  ruleName:
                    description: RuleName is the name of the protection rule
                    type: string
                  updatedAt:
                    description: UpdatedAt is the timestamp when the rule was last
                      updated
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
