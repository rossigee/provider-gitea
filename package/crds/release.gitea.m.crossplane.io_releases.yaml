---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: releases.release.gitea.m.crossplane.io
spec:
  group: release.gitea.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gitea
    kind: Release
    listKind: ReleaseList
    plural: releases
    shortNames:
    - rele
    singular: release
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v2
    schema:
      openAPIV3Schema:
        description: Release is the Schema for the releases API v2 (namespaced)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReleaseSpec defines the desired state of Release
            properties:
              forProvider:
                properties:
                  assets:
                    description: Assets is a list of assets to attach to this release
                    items:
                      description: ReleaseAsset represents a file attached to a release
                      properties:
                        content:
                          description: |-
                            Content is the base64-encoded content of the asset
                            For large files, consider using a separate storage solution
                          type: string
                        contentType:
                          description: ContentType is the MIME type of the asset
                          type: string
                        name:
                          description: Name is the filename for the asset
                          type: string
                        url:
                          description: |-
                            URL is a URL to download the asset content from
                            Alternative to inline content for large files
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  body:
                    description: Body is the description/release notes for the release
                    type: string
                  connectionRef:
                    description: |-
                      V2 Enhancement: Connection reference for multi-tenant support
                      ConnectionRef specifies the Gitea connection to use
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  draft:
                    default: false
                    description: |-
                      Draft indicates if this is a draft release
                      Draft releases are not visible to users without repository write access
                    type: boolean
                  generateNotes:
                    default: false
                    description: GenerateNotes automatically generates release notes
                      from commits and PRs
                    type: boolean
                  name:
                    description: Name is the title of the release
                    type: string
                  owner:
                    description: Owner is the username or organization name that owns
                      the repository
                    type: string
                  prerelease:
                    default: false
                    description: |-
                      Prerelease indicates if this is a prerelease
                      Prereleases are marked as not ready for production and may be unstable
                    type: boolean
                  providerConfigRef:
                    description: |-
                      V2 Enhancement: Namespace-scoped provider config
                      ProviderConfigRef references a ProviderConfig resource in the same namespace
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  repository:
                    description: Repository is the name of the repository
                    type: string
                  tagName:
                    description: TagName is the name of the tag for this release
                    type: string
                  targetCommitish:
                    description: |-
                      TargetCommitish specifies the commitish value that determines where the Git tag is created from
                      Can be any branch or commit SHA. Defaults to repository default branch if omitted
                    type: string
                required:
                - owner
                - repository
                - tagName
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ReleaseStatus defines the observed state of Release
            properties:
              atProvider:
                properties:
                  assets:
                    description: Assets contains information about attached assets
                    items:
                      description: ReleaseAssetObservation contains the observed state
                        of a release asset
                      properties:
                        browserDownloadUrl:
                          description: BrowserDownloadURL is the direct download URL
                            for the asset
                          type: string
                        contentType:
                          description: ContentType is the MIME type of the asset
                          type: string
                        createdAt:
                          description: CreatedAt is the creation timestamp
                          format: date-time
                          type: string
                        downloadCount:
                          description: DownloadCount is the number of times this asset
                            has been downloaded
                          format: int64
                          type: integer
                        id:
                          description: ID is the unique identifier of the asset
                          format: int64
                          type: integer
                        name:
                          description: Name is the filename of the asset
                          type: string
                        size:
                          description: Size is the size of the asset in bytes
                          format: int64
                          type: integer
                        updatedAt:
                          description: UpdatedAt is the last update timestamp
                          format: date-time
                          type: string
                      type: object
                    type: array
                  author:
                    description: Author is the username of the release author
                    type: string
                  body:
                    description: Body is the release notes/description
                    type: string
                  createdAt:
                    description: CreatedAt is the creation timestamp
                    format: date-time
                    type: string
                  draft:
                    description: Draft indicates if this is a draft release
                    type: boolean
                  htmlUrl:
                    description: HTMLURL is the web URL of the release
                    type: string
                  id:
                    description: ID is the unique identifier of the release
                    format: int64
                    type: integer
                  name:
                    description: Name is the title of the release
                    type: string
                  prerelease:
                    description: Prerelease indicates if this is a prerelease
                    type: boolean
                  publishedAt:
                    description: PublishedAt is the publication timestamp
                    format: date-time
                    type: string
                  tagName:
                    description: TagName is the Git tag associated with the release
                    type: string
                  tarballUrl:
                    description: TarballURL is the URL to download the source code
                      as tarball
                    type: string
                  targetCommitish:
                    description: TargetCommitish is the commit SHA or branch that
                      the tag points to
                    type: string
                  uploadUrl:
                    description: UploadURL is the URL for uploading release assets
                    type: string
                  url:
                    description: URL is the API URL of the release
                    type: string
                  zipballUrl:
                    description: ZipballURL is the URL to download the source code
                      as zipball
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
